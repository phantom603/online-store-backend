{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rimraf ./build && tsc && cp ./package.json ./package-lock.json ./build && cd build && npm pkg delete scripts && npm pkg set scripts.start='node -r dotenv/config index.js'",
    "start": "ts-node-dev -r dotenv/config src/index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.3",
    "http-proxy-middleware": "^2.0.6",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "ts-node-dev": "^2.0.0"
  }
}
